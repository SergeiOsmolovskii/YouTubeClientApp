import { VideoInfoConvertPipe } from './video-info-convert.pipe';

describe('VideoInfoConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new VideoInfoConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
