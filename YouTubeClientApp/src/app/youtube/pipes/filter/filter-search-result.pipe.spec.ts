import { FilterSearchResultPipe } from './filter-search-result.pipe';

describe('FilterSearchResultPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSearchResultPipe();
    expect(pipe).toBeTruthy();
  });
});
