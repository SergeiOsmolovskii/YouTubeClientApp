import { BorderDependingOnTheDateDirective } from './border-depending-on-the-date.directive';

describe('BorderDependingOnTheDateDirective', () => {
  it('should create an instance', () => {
    // const directive = new BorderDependingOnTheDateDirective();
    // expect(directive).toBeTruthy();
  });
});
