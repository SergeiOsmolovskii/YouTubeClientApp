export enum colors {
  redColor = '#EB5757',
  greenColor = '#27AE60',
  blueColor = '#2F80ED',
}

export enum sortParam {
  desc = 'desc',
  asc = 'asc' 
};

export enum currentSortParam { 
  date = 'date', 
  count = 'count'
};